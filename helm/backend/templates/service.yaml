apiVersion: v1
kind: Service
metadata:
  name: pdm-backend
  labels:
    app: pdm-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: pdm-backend
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
