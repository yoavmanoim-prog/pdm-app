fastapi==0.111.0
uvicorn==0.29.0

# database
sqlalchemy==2.0.30
psycopg2-binary==2.9.12
alembic==1.13.1

# AWS — S3 storage for documents
boto3==1.34.69

# file handling
python-multipart==0.0.9
pypdf==6.12.2          # merging PDF files

# SVG/PDF conversion — converts uploaded PDFs to SVG for internal storage
pymupdf==1.27.2

# OCR fallback for scanned/image-based PDFs
pytesseract==0.3.10
Pillow==10.4.0

# data validation and settings management
pydantic==2.7.1
pydantic-settings==2.2.1  # reads config from environment variables and .env files

# Prometheus — exposes /metrics so Prometheus can scrape request rate, latency, error rate
prometheus-fastapi-instrumentator==6.1.0  # 8.x pulls starlette 1.x which breaks fastapi 0.111
